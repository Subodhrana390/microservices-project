services:
  - type: web
    name: yourchemist-keep-alive
    plan: free
    runtime: node
    buildCommand: ""        
    startCommand: node alive.js
    autoDeploy: false
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_VERSION
        value: 20
